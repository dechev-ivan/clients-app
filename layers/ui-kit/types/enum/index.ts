export enum UiColor {
    Primary = 'primary',
    Primary100 = 'primary-100',
    Base = 'base',
    BaseWhite = 'base-white',
}

export enum UiSize {
    S = 's',
    M = 'm',
}

export enum UiTags {
    Button = 'button',
    RLink = 'router-link',
}
